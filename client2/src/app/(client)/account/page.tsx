'use client'
import Edit from './components/form'

const Account = () => (
  <section className='flex w-full flex-col gap-6'>
    <h1 className='titulo3 font-semibold'>Editar perfil</h1>
    <Edit />
  </section>
)

export default Account
