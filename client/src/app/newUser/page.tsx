const Page = async () => (
  <div>
    <h1>NEW USER</h1>
    <p>Pagina que se renderiza si se define en la configuracion de NextAuth la redireccion a la pagina new user.</p>
  </div>
)

export default Page
